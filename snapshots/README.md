# Aevryn Snapshots

This directory stores reference snapshots for future comparison.

Use it for validation outputs, milestone reports, Canon Rebuild Test results, and other artifacts worth preserving as historical reference.

Snapshot artifact directories are local generated outputs and are ignored by git.

Do not store cache files, dependency folders, or generated runtime junk here.
